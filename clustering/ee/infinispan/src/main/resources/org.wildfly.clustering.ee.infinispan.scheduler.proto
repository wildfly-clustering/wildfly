package org.wildfly.clustering.ee.infinispan.scheduler;

/**
 * @TypeId(180)
 */
message CancelCommand {
	required	bytes	id	= 1;
}

/**
 * @TypeId(181)
 */
message ScheduleCommand {
	required	bytes	id	= 1;
}
